h3 Blog overview

ul.postList
  each post in posts
    li.post.panel.panel-default
      .panel-heading.clearfix
        h4.pull-left
          a(href="/index/" + post.slug)= post.title
        p.pull-right
          | #{helper.date.format(post.created_at, 'LL')} by admin
      .panel-body= post.content